;; since we're not using the color themes added by Emacs Live, we need
;; to fix our own.

(set-face-background 'git-gutter:unchanged "white"))

(set-face-attribute 'eval-sexp-fu-flash nil
                    :background "lemon chiffon"
                    :foreground nil
                    :bold nil)
